name: keep-alive

# 每 5 分鐘戳一次 Render 健康檢查，避免免費方案閒置休眠
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: {} # 也可手動觸發

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping GymEats
        run: curl -sS --max-time 60 https://gymeats.onrender.com/ || true
